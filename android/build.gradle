plugins {
    id 'java-library'
    id 'maven-publish'
}

group = 'com.apiverve'
version = '1.1.14'

java {
    sourceCompatibility = JavaVersion.VERSION_1_8
    targetCompatibility = JavaVersion.VERSION_1_8
}

dependencies {
    implementation 'org.json:json:20231013'
}

publishing {
    publications {
        maven(MavenPublication) {
            groupId = 'com.apiverve'
            artifactId = 'emaildisposablechecker-api'
            version = '1.1.14'

            from components.java

            pom {
                name = 'Disposable Email Checker'
                description = 'Email Disposable Checker is a simple tool for checking if an email address is disposable. It returns if the email address is disposable or not.'
                url = 'https://apiverve.com'

                licenses {
                    license {
                        name = 'MIT License'
                        url = 'https://opensource.org/licenses/MIT'
                    }
                }

                developers {
                    developer {
                        id = 'apiverve'
                        name = 'APIVerve'
                        email = 'hello@apiverve.com'
                    }
                }

                scm {
                    connection = 'scm:git:git://github.com/apiverve/emaildisposablechecker-api.git'
                    developerConnection = 'scm:git:ssh://github.com/apiverve/emaildisposablechecker-api.git'
                    url = 'https://github.com/apiverve/emaildisposablechecker-api'
                }
            }
        }
    }
}
